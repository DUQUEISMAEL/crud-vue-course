title: Crud Vue
description: creando una Crud en vue
template:
  repo: crud-vue-template
  name: crud-vue
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
